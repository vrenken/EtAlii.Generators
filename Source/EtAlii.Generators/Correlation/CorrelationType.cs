namespace EtAlii.Generators
{
    /// <summary>
    /// Currently supported keys for (Log-) correlation.
    /// </summary>
    // Will be used 'as is' as the key.
    // Do not add to many, they should be meaningful.
    public enum CorrelationType
    {
        SourceGeneration,
    }
}
